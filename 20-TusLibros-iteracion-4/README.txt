Para levantar el servidor, en un workspace, ejecutar

StoreRestInterface listeningOn: 8080.


Para levantar la ventana de cliente, ejecutar

LogInWindow open.


Para el uso, hay un usuario por defecto, con nombre de usuario "admin" y contraseña "admin" (sin las comillas dobles).